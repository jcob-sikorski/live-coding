# The Identity Function
# Write a function identity that takes an argument of any type and returns it.
# Use the Python 3.12+ syntax to ensure the return type matches the input type.

def identity[T](arg: T) -> T:
    return arg
