# The Squarer
# Write a lambda function that takes one argument and returns its square.
# Assign it to a variable named square and test it with the number 12.

squarer = (lambda x: x**2)

print(squarer(12))